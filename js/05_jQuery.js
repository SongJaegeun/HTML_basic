
// jQuery CDN_을 이용해 사용 가능

function my_func() {
    // 0. jQuery_의 핵심은 selector
    // 1. 전체 선택자(universe selector)
    // $("*").css("color", "red") // 모든 element_를 일괄 선택
    // 2. 태그 선택자(tag selector)
    // $("li").remove()
    // 3. ID 선택자(id selector, Unique)
    // $("#haha").text('제주') // 인자가 없으면 값을 알아_오라는 의미, 인자가 있으면 값을 변경_하라는 의미
    // 4. 클래스 선택자(class selector, UnUnique)
    // $(".region").css('background-color','bisque')
    // 5. 구조 선택자(자식_선택자 '>' , 후손_선택자 ' ')
    // $("div ol li").css('color', 'steelblue')
    // 6. 구조 선택자(형제_선택자)
    // $("#haha + li").remove()
    // $('#hong ~ li').remove()
    // 7. 속성 선택자
    // $('[type]').remove()
    
}